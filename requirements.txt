# GoldRegimeX2 dependencies
# Optuna removed as part of the grid-search-plateau migration (was: optuna>=3.5).
numpy>=1.24
pandas>=2.0
hmmlearn>=0.3
xgboost>=2.0
scikit-learn>=1.3
joblib>=1.3
onnx>=1.15
onnxmltools>=1.12
skl2onnx>=1.16
pyarrow>=14.0
matplotlib>=3.8
onnxconverter-common>=1.14
metatrader5
tensorflow>=2.13

# Optional (not required): speeds up the Numba backtest engine in the
# grid-search-plateau pipeline. The engine detects Numba at import time and
# falls back to pure Python when it is missing, so this is safe to omit —
# recommended if your Python version has no compatible numba wheel.
# numba>=0.59
