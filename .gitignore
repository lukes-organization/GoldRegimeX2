# Secrets
.env

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/

# Data and models (large files)
data/raw/
data/processed/
models/*.pkl
models/*.joblib
models/*.onnx
models/study.db



# Logs
logs/

# Compiled MQL5 binary (source .mq5 is tracked)
mql5/*.ex5

# IDE
.vscode/settings.json
.idea/
nul
.kilo/

# Too large for GitHub (>100 MB hard limit)
pipeline_verification_bundle/reports/candidate_portfolios.json
pipeline_verification_bundle/reports/candidate_portfolios.json
pipeline_verification_bundle/reports/candidate_trades_export.csv
pipeline_verification_bundle/reports/candidate_trades_export.csv

# Also exclude any regenerated copies with size suffixes / timestamps
**/candidate_portfolios*.json
**/candidate_trades_export*.csv

cd C:\GoldRegime_X

@"

# --- Files >100 MB that GitHub rejects ---
pipeline_verification_bundle/reports/candidate_portfolios.json
pipeline_verification_bundle/reports/candidate_trades_export.csv
"@ | Add-Content -Path .gitignore -Encoding UTF8

git add .gitignore
git commit -m "Ignore files exceeding GitHub 100MB limit"
pipeline_verification_bundle/reports/observability/*
!pipeline_verification_bundle/reports/observability/.gitkeep
